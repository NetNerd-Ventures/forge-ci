name: migrate
on:
  push:
    branches: [main]
permissions:
  contents: read
  issues: write
jobs:
  migrate:
    uses: NetNerd-Ventures/forge-ci/.github/workflows/migrate.yml@v1
    with:
      forge-ci-ref: v1
      integration-branch: main
    secrets: inherit
