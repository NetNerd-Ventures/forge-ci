# forge-ci

Reusable GitHub Actions workflows and scripts for the forge deploy pipeline.
Consumers are generated by the `forge` Claude Code plugin; see
`docs/specs/2026-09-18-forge-ci-pipeline-design.md` there.

This repo holds no application code. It publishes six `workflow_call`
workflows, the scripts they run, two branch-ruleset templates, and a test
harness that lints and unit-tests everything before every push. Consumers
`uses:` the workflows at a pinned tag; nothing here runs against a
consumer's secrets or data except through the inputs/secrets a consumer
explicitly passes.

## Why this repo is public

Reusable GitHub Actions workflows can only be called from another repo in
the same GitHub Actions run's context when the *called* workflow's repo is
public, or when the calling repo has explicit access to a private one. To
avoid managing per-repo access grants for every consumer under
`NetNerd-Ventures`, this repo is public. Nothing secret lives in it: no
values, only secret *names* consumers must set, and the workflows never log
inputs that could carry a secret. Being public also means the workflow
source is auditable by anyone before a consumer wires it in.

## The `forge-ci-ref` rule

Every reusable workflow declares a required `forge-ci-ref` input. Workflows
that run scripts (`gates.yml`'s drift job, `cross-model-review.yml`,
`migrate.yml`, `deploy-status.yml`) check forge-ci out into `.forge-ci/` at
that ref before running any script, so those jobs always execute the scripts
that shipped with the exact `forge-ci-ref` they were called with — not
whatever the caller's `@vN` tag happens to resolve to at call time for
supporting scripts, and not the workflow file's own commit. `open-pr.yml`
and `promote.yml` declare `forge-ci-ref` too, for a uniform caller contract
across all six workflows, but run only inline shell (`gh`/`git`), never
scripts under `scripts/`, so they never check it out. Consumers therefore
write the same ref twice:
once in `uses: .../gates.yml@v1` (which version of the *workflow* runs) and
once in `with: forge-ci-ref: v1` (which version of the *scripts* it checks
out). Keep both in sync; a probe repo confirmed GitHub resolves the `uses:`
tag independently of the input.

Every script under `scripts/` finds the consumer repo via `REPO_ROOT`
(default: the process's working directory), never via its own parent
directory, because it runs from the nested `.forge-ci/scripts/` checkout
with the consumer repo as the working directory.

## Check-run names and ruleset contexts

A check run produced by a called (reusable) workflow's job is named
`<caller job id> / <callee job name>`. The forge templates fix the caller
job ids to `gates` and `review`, so the check-run names a branch ruleset
must require are:

- `gates / typecheck`
- `gates / unit tests`
- `gates / migration drift`
- `gates / gitleaks`
- `gates / actionlint`
- `review / cross-model review` (two-tier only; see below)

`scripts/apply-rulesets.sh` renders and applies these automatically — see
"Ruleset script usage" below.

## Two modes

**Two-tier**: a `staging` integration branch feeds a `main` production
branch through `/promote`. Cross-model review is `required: true` — the
check goes red and blocks merge if a vendor model flags a problem, and
`promote.yml` opens the staging→main PR. Canonical example: the five
callers plus `promote.yml` under
`test/fixtures/consumer-two-tier/.github/workflows/`.

**Single-tier**: `main` is both the integration and production branch.
There is no `promote.yml` caller. Cross-model review is `required: false`
(the workflow's own `required` input flows into the script as
`--advisory`): the review still runs and still posts its sticky PR comment,
but the check never turns red and the step summary is labelled ADVISORY
instead of blocking. Canonical example: `test/fixtures/consumer-single-tier/.github/workflows/`.

Both fixture trees are real, valid caller workflows — `test/run.sh` and
`actionlint` lint them for syntax on every push
(`.github/workflows/self-test.yml`). actionlint does not validate a fixture's
`with:` keys against a remote `owner/repo@ref` callee's declared inputs, so
this does not catch a fixture drifting out of sync with the reusable
workflows' actual inputs; it does keep the fixtures syntactically valid and
they remain the canonical, runnable examples of both modes.

## Workflows

Each reusable workflow's `on.workflow_call.inputs` / `.secrets` block is the
source of truth; the tables below are read from it directly
(`.github/workflows/<name>.yml`).

### `gates.yml`

Caller job id: `gates`. Runs typecheck, unit tests, migration drift check,
gitleaks and actionlint as named jobs/steps that produce the
`gates / <job name>` check runs listed above.

| Input | Type | Default | Required |
| --- | --- | --- | --- |
| `forge-ci-ref` | string | — | yes |
| `package-manager` | string | `npm` | no |
| `node-version` | string | `22` | no |
| `node-options` | string | `''` | no (`typecheck` job only) |
| `prebuild` | string | `''` | no |
| `typecheck-command` | string | `npm run typecheck` | no |
| `test-command` | string | `npm test` | no |
| `has-migrations` | boolean | `true` | no |
| `migrations-dir` | string | `supabase/migrations` | no |
| `rls-check-command` | string | `''` | no |

No secrets.

### `open-pr.yml`

Caller job id: `open-pr`. Opens/refreshes the feature-branch→integration-branch
PR: the branch is opened as a draft when it is prefixed with `draft-prefix`
(default `wip/`), and auto-merge is enabled when `auto-merge: true` and the
PR is not a draft. `promote.yml` owns the integration→production PR.

| Input | Type | Default | Required |
| --- | --- | --- | --- |
| `forge-ci-ref` | string | — | yes |
| `integration-branch` | string | — | yes |
| `production-branch` | string | `main` | no |
| `auto-merge` | boolean | `true` | no |
| `pipeline-enabled` | boolean | `true` | no |
| `draft-prefix` | string | `wip/` | no |
| `migrations-dir` | string | `supabase/migrations` | no |

| Secret | Required |
| --- | --- |
| `app-id` | yes |
| `app-private-key` | yes |

### `cross-model-review.yml`

Caller job id: `review`. Job name `cross-model review`. Posts one sticky PR
comment from a vendor that did not write the change.

| Input | Type | Default | Required |
| --- | --- | --- | --- |
| `forge-ci-ref` | string | — | yes |
| `required` | boolean | `true` | no |
| `config-path` | string | `''` | no |

| Secret | Required |
| --- | --- |
| `anthropic-api-key` | no |
| `openai-api-key` | no |
| `xai-api-key` | no |

### `migrate.yml`

Caller job id: `migrate`. Applies pending Supabase migrations and updates
the ledger. Declares **no** `secrets:` block — it reads the
environment-scoped `secrets.DATABASE_URL` from the GitHub Environment
matching the branch (`staging-db` for the integration branch,
`production-db` for the production branch), so its caller must pass
`secrets: inherit` rather than naming secrets explicitly.

| Input | Type | Default | Required |
| --- | --- | --- | --- |
| `forge-ci-ref` | string | — | yes |
| `integration-branch` | string | — | yes |
| `production-branch` | string | `main` | no |
| `migrations-dir` | string | `supabase/migrations` | no |
| `ledger-table` | string | `_migration_log` | no |
| `runbook-path` | string | `docs/runbooks/developer-workflow.md` | no |

No `secrets:` in the workflow_call block; caller must use `secrets:
inherit`.

### `deploy-status.yml`

Caller job id: `status`. Waits for the matching Vercel deployment per
project and opens/closes a failure issue.

| Input | Type | Default | Required |
| --- | --- | --- | --- |
| `forge-ci-ref` | string | — | yes |
| `integration-branch` | string | — | yes |
| `production-branch` | string | `main` | no |
| `vercel-team-id` | string | — | yes |
| `vercel-projects` | string | — | yes |

`vercel-projects` is a JSON string: an array of `{name, id, paths}`
objects, one per Vercel project watched on this push (`paths` is a
comma-separated list of path prefixes that project's deploy depends on).

| Secret | Required |
| --- | --- |
| `vercel-token` | yes |

### `promote.yml`

Caller job id: `promote`. Two-tier only: opens the staging→production PR.
No single-tier caller exists for this workflow (single-tier has no
promotion step; `main` is both branches).

| Input | Type | Default | Required |
| --- | --- | --- | --- |
| `forge-ci-ref` | string | — | yes |
| `integration-branch` | string | — | yes |
| `production-branch` | string | `main` | no |
| `migrations-dir` | string | `supabase/migrations` | no |
| `pr-title` | string | `Promote staging to production` | no |

| Secret | Required |
| --- | --- |
| `app-id` | yes |
| `app-private-key` | yes |

## Consumer prerequisites

Before the callers above can go green, a consumer repo needs:

- **`.github/pull_request_template.md`** — `open-pr.yml` reads this file and
  substitutes its migrations list into the line starting with the marker
  `<!-- migrations:`. Without the file (or without that marker line), the PR
  body is not populated with the migrations list.
- **`CLAUDE.md` on the base/integration branch** — `scripts/cross-model-review.mjs`
  reads `<base>:CLAUDE.md` (the base branch's committed copy, via `git show`,
  not the PR head's) for the project rules it hands the reviewing model. A
  missing file crashes the script before a verdict is produced, failing the
  `review / cross-model review` check outright — in `required: true`
  (two-tier) mode that blocks merge; in advisory mode the check still fails,
  it just does not gate the ruleset.
- **GitHub Environments** — `staging-db` and `production-db` (two-tier) or
  `production-db` only (single-tier), each holding the `DATABASE_URL` secret
  `migrate.yml` reads. See "Secrets" below for the exact scoping.
- **A lockfile matching `package-manager`** — `gates.yml`'s `setup-node`
  step caches on it and runs `npm ci` (or the bun equivalent) against it; an
  `npm` consumer needs `package-lock.json`, a `bun` consumer needs
  `bun.lockb`/`bun.lock`.
- **The org secrets and `VERCEL_TOKEN`** already listed under "Secrets"
  below.

## Ruleset script usage

```bash
scripts/apply-rulesets.sh --repo owner/name --mode two-tier|single-tier \
  --integration <branch> --production <branch> \
  [--extra-gate <job name>]... [--dry-run]
```

Renders `rulesets/integration.json` (two-tier only) and
`rulesets/production.json` from the templates in `rulesets/`, filling in
the required-status-check contexts listed above (plus `review / cross-model
review` for two-tier, plus any `--extra-gate` names), and applies them with
`gh api` against `repos/<owner>/<name>/rulesets`. Idempotent: a ruleset
already named `forge-ci: <branch>` is updated (`PUT`) in place rather than
duplicated; a new one is created (`POST`). Single-tier adds the merge-queue
rule to the one ruleset it creates, since there is no separate integration
tier to queue into. `--dry-run` prints the rendered ruleset JSON and exits
without calling `gh`.

## Secrets

Org-level secrets (`gh secret set <NAME> --org NetNerd-Ventures --visibility
selected --repos <list>`), names only — values are never read or written by
anything in this repo:

- `FORGE_CI_APP_ID`
- `FORGE_CI_APP_PRIVATE_KEY`
- `FORGE_REVIEW_OPENAI_API_KEY`
- `FORGE_REVIEW_XAI_API_KEY`
- `FORGE_REVIEW_ANTHROPIC_API_KEY`

Per-repo secrets, set directly on the consumer repo:

- `DATABASE_URL`, scoped by GitHub Environment: two-tier consumers need both
  `staging-db` and `production-db` (`migrate.yml` picks one by branch).
  Single-tier consumers need only `production-db` — `migrate.yml` tests the
  production branch first, and with `integration-branch == production-branch`
  that branch always resolves to `production-db`, so `staging-db` is never
  selected.
- `VERCEL_TOKEN`.

### Rotation

```bash
gh secret set <NAME> --org NetNerd-Ventures --visibility selected --repos <list>
```

Before rotating an org secret, check whether any consumer overrides it with
a per-repo fallback of the same name (a per-repo secret always wins over an
org one of the same name), by grepping that consumer's deviations:

```bash
jq '.deviations' <consumer-repo>/.forge/config.json
```

Any repo listed there for a secret name needs its own `gh secret set`
alongside the org rotation, or it will keep using its stale per-repo value.

## Tag policy

- `v1.x.y` (e.g. `v1.0.0`) is an immutable release tag — never force-moved.
- `v1` is a moving major tag, force-updated (`git tag -f v1 <vX.Y.Z>`) to
  point at the latest `v1.x.y` release. Consumers pin `uses: .../<workflow>.yml@v1`
  to always track the latest 1.x release, or pin an exact `@v1.x.y` to
  freeze.

## Local development

```bash
bash test/run.sh                                          # JS + shell harnesses
actionlint .github/workflows/*.yml test/fixtures/*/.github/workflows/*.yml
shellcheck -x scripts/*.sh test/*.sh
```

Node 22, no npm dependencies. See `CLAUDE.md` for the working-directory /
`REPO_ROOT` contract scripts must follow.
